[package]
name = "cyclotron"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2.152"
rand = "0.8.5"
goblin = "0.5"
# sv-parser = "0.13.3"

[lib]
name = "cyclotron"
crate-type = ["dylib"]
